kind = "web"
previewPath = "/"
title = "AURELIX SYSTEMS"
version = "1.0.0"
id = "artifacts/aurelix"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26209

[services.development]
run = "pnpm --filter @workspace/aurelix run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aurelix", "run", "build" ]
publicDir = "artifacts/aurelix/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26209"
BASE_PATH = "/"
